<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:nav="clr-namespace:Joufflu.Navigation.Controls">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Joufflu.Navigation;component/Controls/Overlay.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <BooleanToVisibilityConverter x:Key="BoolToVis" />

    <!--  ============================ OverlayContainer ============================  -->
    <Style TargetType="{x:Type nav:OverlayContainer}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type nav:OverlayContainer}">
                    <Grid Background="{TemplateBinding Background}">
                        <!--
                            The wrapped application. Padding insets it only, so the overlays
                            stay full-bleed over the whole surface.
                        -->
                        <ContentPresenter Margin="{TemplateBinding Padding}" />

                        <!--  Modal overlay stack  -->
                        <ItemsControl
                            ItemTemplate="{StaticResource OverlayTemplate}"
                            ItemsSource="{Binding Overlays.Overlays, RelativeSource={RelativeSource TemplatedParent}}"
                            Visibility="{Binding Overlays.HasOverlays, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVis}}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Grid />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
