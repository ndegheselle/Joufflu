<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:fonts="clr-namespace:Joufflu.Assets.Fonts;assembly=Joufflu"
    xmlns:joufflu="clr-namespace:Joufflu;assembly=Joufflu"
    xmlns:local="clr-namespace:Joufflu.FileExplorer.Controls">

    <!--
        Node visuals and context menus shared by every control displaying explorer nodes. The templates are implicit
        (keyed by DataType) so that a consumer can template its own IExplorerNode implementations the same way.
    -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Joufflu.FileExplorer;component/Controls/ExplorerContextMenu.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <DataTemplate DataType="{x:Type local:PhysicalFile}">
        <StackPanel joufflu:Spacing.Gap="4" Orientation="Horizontal">
            <fonts:FontIcon Text="{x:Static fonts:LucideFontIcons.File}" />
            <TextBlock Text="{Binding Name}" />
        </StackPanel>
    </DataTemplate>
    <DataTemplate DataType="{x:Type local:PhysicalDirectory}">
        <StackPanel joufflu:Spacing.Gap="4" Orientation="Horizontal">
            <fonts:FontIcon Text="{x:Static fonts:LucideFontIcons.Folder}" />
            <TextBlock Text="{Binding Name}" />
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>
