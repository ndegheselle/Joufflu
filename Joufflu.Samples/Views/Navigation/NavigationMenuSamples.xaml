<UserControl
    x:Class="Joufflu.Samples.Views.Navigation.NavigationMenuSamples"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:Joufflu.Samples.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fonts="clr-namespace:Joufflu.Assets.Fonts;assembly=Joufflu"
    xmlns:joufflu="clr-namespace:Joufflu;assembly=Joufflu"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:nav="clr-namespace:Joufflu.Navigation.Controls;assembly=Joufflu.Navigation"
    xmlns:vm="clr-namespace:Joufflu.Samples.Views.Navigation"
    d:DataContext="{d:DesignInstance Type=vm:NavigationMenuSamplesViewModel}"
    mc:Ignorable="d">
    <ScrollViewer Padding="{StaticResource {x:Static joufflu:Dimensions.SpacingThickness}}" VerticalScrollBarVisibility="Auto">
        <StackPanel joufflu:Spacing.Gap="{StaticResource {x:Static joufflu:Dimensions.SpacingThickness}}">
            <TextBlock Style="{StaticResource H1}" Text="Navigation menu" />

            <c:CodeExample
                Title="Collapsible side menu"
                Code="{Binding Code}"
                Description="The collapsible side menu in isolation, plugged into its own navigator. Use the chevron to collapse it to an icons-only rail.">
                <Border
                    Height="280"
                    BorderBrush="{DynamicResource {x:Static joufflu:Brushes.BorderBrush}}"
                    BorderThickness="{DynamicResource {x:Static joufflu:Dimensions.BorderThickness}}"
                    CornerRadius="{DynamicResource {x:Static joufflu:Dimensions.CornerRadius}}"
                    SnapsToDevicePixels="True">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <nav:NavigationMenu Navigator="{Binding DemoNavigator}">
                            <nav:NavigationMenu.Header>
                                <StackPanel
                                    VerticalAlignment="Center"
                                    joufflu:Spacing.Gap="8"
                                    Orientation="Horizontal">
                                    <fonts:FontIcon FontSize="20" Text="{x:Static fonts:LucideFontIcons.Rocket}" />
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontWeight="SemiBold"
                                        Text="Joufflu" />
                                </StackPanel>
                            </nav:NavigationMenu.Header>
                            <nav:NavigationTitle>Demo</nav:NavigationTitle>
                            <nav:NavigationItem TargetType="{x:Type vm:DemoHomePage}">
                                <nav:NavigationItem.Icon>
                                    <fonts:FontIcon Text="{x:Static fonts:LucideFontIcons.Home}" />
                                </nav:NavigationItem.Icon>
                                Home
                            </nav:NavigationItem>
                            <nav:NavigationGroup Header="Parent" IsExpanded="True">
                                <nav:NavigationGroup.Icon>
                                    <fonts:FontIcon Text="{x:Static fonts:LucideFontIcons.Folder}" />
                                </nav:NavigationGroup.Icon>
                                <nav:NavigationItem TargetType="{x:Type vm:DemoSubmenu1Page}">Submenu 1</nav:NavigationItem>
                                <nav:NavigationItem TargetType="{x:Type vm:DemoSubmenu2Page}">Submenu 2</nav:NavigationItem>
                                <nav:NavigationGroup Header="Parent" IsExpanded="True">
                                    <nav:NavigationGroup.Icon>
                                        <fonts:FontIcon Text="{x:Static fonts:LucideFontIcons.Folder}" />
                                    </nav:NavigationGroup.Icon>
                                    <nav:NavigationItem TargetType="{x:Type vm:DemoNestedSubmenu1Page}">Submenu 1</nav:NavigationItem>
                                    <nav:NavigationItem TargetType="{x:Type vm:DemoNestedSubmenu2Page}">Submenu 2</nav:NavigationItem>
                                </nav:NavigationGroup>
                            </nav:NavigationGroup>
                            <nav:NavigationItem TargetType="{x:Type vm:DemoSettingsPage}">
                                <nav:NavigationItem.Icon>
                                    <fonts:FontIcon Text="{x:Static fonts:LucideFontIcons.Settings}" />
                                </nav:NavigationItem.Icon>
                                Settings
                            </nav:NavigationItem>
                        </nav:NavigationMenu>

                        <ContentControl
                            Grid.Column="1"
                            Margin="16"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Content="{Binding DemoNavigator.CurrentPage}" />
                    </Grid>
                </Border>
            </c:CodeExample>
        </StackPanel>
    </ScrollViewer>
</UserControl>
