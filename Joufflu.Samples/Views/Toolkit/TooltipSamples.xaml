<UserControl
    x:Class="Joufflu.Samples.Views.Toolkit.TooltipSamples"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:Joufflu.Samples.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fonts="clr-namespace:Joufflu.Assets.Fonts;assembly=Joufflu"
    xmlns:joufflu="clr-namespace:Joufflu;assembly=Joufflu"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Joufflu.Toolkit;assembly=Joufflu"
    xmlns:vm="clr-namespace:Joufflu.Samples.Views.Toolkit"
    d:DataContext="{d:DesignInstance Type=vm:TooltipSamplesViewModel}"
    mc:Ignorable="d">
    <ScrollViewer Padding="{StaticResource {x:Static joufflu:Dimensions.SpacingThickness}}" VerticalScrollBarVisibility="Auto">
        <StackPanel toolkit:Spacing.Gap="{StaticResource {x:Static joufflu:Dimensions.SpacingThickness}}">
            <TextBlock Style="{StaticResource H1}" Text="Tooltip" />

            <c:CodeExample
                Title="Tooltip.Placement"
                Code="{Binding PlacementCode}"
                Description="Set Tooltip.Content on any element to show a themed tooltip on hover. Tooltip.Placement chooses the side it sits on — Top, Bottom, Left or Right — and it still flips automatically near a screen edge.">
                <WrapPanel toolkit:Spacing.Gap="8,8">
                    <Button
                        Content="Top"
                        toolkit:Tooltip.Content="Placed above"
                        toolkit:Tooltip.Placement="Top" />
                    <Button
                        Content="Bottom"
                        toolkit:Tooltip.Content="Placed below"
                        toolkit:Tooltip.Placement="Bottom" />
                    <Button
                        Content="Left"
                        toolkit:Tooltip.Content="Placed left"
                        toolkit:Tooltip.Placement="Left" />
                    <Button
                        Content="Right"
                        toolkit:Tooltip.Content="Placed right"
                        toolkit:Tooltip.Placement="Right" />
                </WrapPanel>
            </c:CodeExample>

            <c:CodeExample
                Title="Custom content"
                Code="{Binding ContentCode}"
                Description="Tooltip.Content is an object, so it accepts arbitrary XAML — not just text.">
                <WrapPanel toolkit:Spacing.Gap="8,8">
                    <Button Content="Rich tooltip" toolkit:Tooltip.Placement="Bottom">
                        <toolkit:Tooltip.Content>
                            <StackPanel Orientation="Horizontal" toolkit:Spacing.Gap="8">
                                <fonts:FontIcon Text="{x:Static fonts:LucideFontIcons.Info}" />
                                <TextBlock Text="Arbitrary content, icons included." />
                            </StackPanel>
                        </toolkit:Tooltip.Content>
                    </Button>
                </WrapPanel>
            </c:CodeExample>
        </StackPanel>
    </ScrollViewer>
</UserControl>
