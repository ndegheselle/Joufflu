<UserControl
    x:Class="Joufflu.Samples.Views.Toolkit.ThemeCustomizer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Joufflu.Controls;assembly=Joufflu"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:inputs="clr-namespace:Joufflu.Inputs.Controls;assembly=Joufflu.Inputs"
    xmlns:joufflu="clr-namespace:Joufflu;assembly=Joufflu"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Joufflu.Samples.Views.Toolkit"
    d:DataContext="{d:DesignInstance Type=vm:ThemeCustomizerViewModel}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!--  One editable colour row: swatch/hex editor + label  -->
        <DataTemplate x:Key="ColorRowTemplate" DataType="{x:Type vm:ThemeColorEntry}">
            <Grid Margin="0,3" joufflu:Spacing.HorizontalGap="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <inputs:ColorPicker Grid.Column="0" Color="{Binding Color, Mode=TwoWay}" />
                <TextBlock
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{Binding Label}"
                    TextTrimming="CharacterEllipsis" />
            </Grid>
        </DataTemplate>

        <!--  One editable dimension row: label + slider + value  -->
        <DataTemplate x:Key="DimensionRowTemplate" DataType="{x:Type vm:ThemeDimensionEntry}">
            <Grid Margin="0,3" joufflu:Spacing.HorizontalGap="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Text="{Binding Label}" />
                <Slider
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    IsSnapToTickEnabled="True"
                    Maximum="{Binding Maximum}"
                    Minimum="{Binding Minimum}"
                    TickFrequency="1"
                    Value="{Binding Value, Mode=TwoWay}" />
                <TextBlock
                    Grid.Column="2"
                    MinWidth="52"
                    VerticalAlignment="Center"
                    TextAlignment="Right">
                    <Run Text="{Binding Value, StringFormat=0}" />
                    <Run Foreground="{DynamicResource {x:Static joufflu:Brushes.Foreground100Brush}}" Text="{Binding Unit, Mode=OneWay}" />
                </TextBlock>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="{StaticResource {x:Static joufflu:Dimensions.SpacingThickness}}" joufflu:Spacing.VerticalGap="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <StackPanel Grid.Row="0">
            <TextBlock Style="{StaticResource H1}" Text="Customize theme" />
            <TextBlock
                Foreground="{DynamicResource {x:Static joufflu:Brushes.Foreground100Brush}}"
                Text="Tweak colours and dimensions and watch the whole gallery update live. Copy or save the generated dictionary and merge it into your app after the Joufflu resources to apply it."
                TextWrapping="Wrap" />
        </StackPanel>

        <!--  Editor | Preview  -->
        <Grid Grid.Row="1" joufflu:Spacing.HorizontalGap="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="440" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  Editor  -->
            <Border Grid.Column="0" Style="{StaticResource Card}">
                <DockPanel>
                    <StackPanel
                        DockPanel.Dock="Top"
                        joufflu:Spacing.HorizontalGap="8"
                        Orientation="Horizontal">
                        <Button Command="{Binding CopyCommand}" Style="{StaticResource Primary}">Copy XAML</Button>
                        <Button Command="{Binding SaveCommand}" Style="{StaticResource Secondary}">Save .xaml…</Button>
                        <Button Command="{Binding ResetCommand}" Style="{StaticResource Ghost}">Reset</Button>
                    </StackPanel>

                    <ScrollViewer Margin="0,12,0,0" VerticalScrollBarVisibility="Auto">
                        <StackPanel joufflu:Spacing.VerticalGap="16">
                            <!--  Colours  -->
                            <StackPanel joufflu:Spacing.VerticalGap="10">
                                <TextBlock Style="{StaticResource H2}" Text="Colours" />
                                <ItemsControl ItemsSource="{Binding ColorGroups}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:ThemeColorGroup}">
                                            <StackPanel Margin="0,0,0,10">
                                                <TextBlock Style="{StaticResource H4}" Text="{Binding Header}" />
                                                <ItemsControl ItemTemplate="{StaticResource ColorRowTemplate}" ItemsSource="{Binding Colors}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>

                            <!--  Dimensions  -->
                            <StackPanel joufflu:Spacing.VerticalGap="10">
                                <TextBlock Style="{StaticResource H2}" Text="Dimensions" />
                                <ItemsControl ItemsSource="{Binding DimensionGroups}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:ThemeDimensionGroup}">
                                            <StackPanel Margin="0,0,0,10">
                                                <TextBlock Style="{StaticResource H4}" Text="{Binding Header}" />
                                                <ItemsControl ItemTemplate="{StaticResource DimensionRowTemplate}" ItemsSource="{Binding Dimensions}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <!--  Preview  -->
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <StackPanel joufflu:Spacing.VerticalGap="12">

                    <!--  Semantic swatches  -->
                    <Border Style="{StaticResource Card}">
                        <StackPanel joufflu:Spacing.VerticalGap="10">
                            <TextBlock Style="{StaticResource H3}" Text="Palette" />
                            <WrapPanel joufflu:Spacing.HorizontalGap="8" joufflu:Spacing.VerticalGap="8">
                                <Border Width="120" Height="56" Background="{DynamicResource {x:Static joufflu:Brushes.PrimaryBrush}}" CornerRadius="{StaticResource {x:Static joufflu:Dimensions.CornerRadius}}">
                                    <TextBlock Margin="8" VerticalAlignment="Bottom" Foreground="{DynamicResource {x:Static joufflu:Brushes.PrimaryContentBrush}}" Text="Primary" />
                                </Border>
                                <Border Width="120" Height="56" Background="{DynamicResource {x:Static joufflu:Brushes.SecondaryBrush}}" CornerRadius="{StaticResource {x:Static joufflu:Dimensions.CornerRadius}}">
                                    <TextBlock Margin="8" VerticalAlignment="Bottom" Foreground="{DynamicResource {x:Static joufflu:Brushes.SecondaryContentBrush}}" Text="Secondary" />
                                </Border>
                                <Border Width="120" Height="56" Background="{DynamicResource {x:Static joufflu:Brushes.SuccessBrush}}" CornerRadius="{StaticResource {x:Static joufflu:Dimensions.CornerRadius}}">
                                    <TextBlock Margin="8" VerticalAlignment="Bottom" Foreground="{DynamicResource {x:Static joufflu:Brushes.SuccessContentBrush}}" Text="Success" />
                                </Border>
                                <Border Width="120" Height="56" Background="{DynamicResource {x:Static joufflu:Brushes.InfoBrush}}" CornerRadius="{StaticResource {x:Static joufflu:Dimensions.CornerRadius}}">
                                    <TextBlock Margin="8" VerticalAlignment="Bottom" Foreground="{DynamicResource {x:Static joufflu:Brushes.InfoContentBrush}}" Text="Info" />
                                </Border>
                                <Border Width="120" Height="56" Background="{DynamicResource {x:Static joufflu:Brushes.WarningBrush}}" CornerRadius="{StaticResource {x:Static joufflu:Dimensions.CornerRadius}}">
                                    <TextBlock Margin="8" VerticalAlignment="Bottom" Foreground="{DynamicResource {x:Static joufflu:Brushes.WarningContentBrush}}" Text="Warning" />
                                </Border>
                                <Border Width="120" Height="56" Background="{DynamicResource {x:Static joufflu:Brushes.DangerBrush}}" CornerRadius="{StaticResource {x:Static joufflu:Dimensions.CornerRadius}}">
                                    <TextBlock Margin="8" VerticalAlignment="Bottom" Foreground="{DynamicResource {x:Static joufflu:Brushes.DangerContentBrush}}" Text="Danger" />
                                </Border>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <!--  Buttons  -->
                    <Border Style="{StaticResource Card}">
                        <StackPanel joufflu:Spacing.VerticalGap="10">
                            <TextBlock Style="{StaticResource H3}" Text="Buttons" />
                            <WrapPanel joufflu:Spacing.HorizontalGap="8" joufflu:Spacing.VerticalGap="8">
                                <Button>Default</Button>
                                <Button Style="{StaticResource Primary}">Primary</Button>
                                <Button Style="{StaticResource Secondary}">Secondary</Button>
                                <Button Style="{StaticResource Success}">Success</Button>
                                <Button Style="{StaticResource Danger}">Danger</Button>
                                <Button Style="{StaticResource Ghost}">Ghost</Button>
                                <Button IsEnabled="False">Disabled</Button>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <!--  Sizes (one control per size variant so the height/font-size sliders have visible targets)  -->
                    <Border Style="{StaticResource Card}">
                        <StackPanel joufflu:Spacing.VerticalGap="10">
                            <TextBlock Style="{StaticResource H3}" Text="Sizes" />
                            <WrapPanel VerticalAlignment="Center" joufflu:Spacing.HorizontalGap="8" joufflu:Spacing.VerticalGap="8">
                                <Button joufflu:ControlProperties.Size="xs" Style="{StaticResource Primary}">Extra small</Button>
                                <Button joufflu:ControlProperties.Size="sm" Style="{StaticResource Primary}">Small</Button>
                                <Button joufflu:ControlProperties.Size="md" Style="{StaticResource Primary}">Medium</Button>
                                <Button joufflu:ControlProperties.Size="lg" Style="{StaticResource Primary}">Large</Button>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <!--  Inputs  -->
                    <Border Style="{StaticResource Card}">
                        <StackPanel joufflu:Spacing.VerticalGap="10">
                            <TextBlock Style="{StaticResource H3}" Text="Inputs" />
                            <WrapPanel joufflu:Spacing.HorizontalGap="12" joufflu:Spacing.VerticalGap="10">
                                <TextBox Width="180" Text="Editable text" />
                                <ComboBox Width="160" SelectedIndex="0">
                                    <ComboBoxItem>First option</ComboBoxItem>
                                    <ComboBoxItem>Second option</ComboBoxItem>
                                    <ComboBoxItem>Third option</ComboBoxItem>
                                </ComboBox>
                                <CheckBox VerticalAlignment="Center" IsChecked="True" Content="Checkbox" />
                                <StackPanel VerticalAlignment="Center" joufflu:Spacing.HorizontalGap="8" Orientation="Horizontal">
                                    <RadioButton IsChecked="True" Content="One" />
                                    <RadioButton Content="Two" />
                                </StackPanel>
                            </WrapPanel>
                            <Slider Maximum="100" Minimum="0" Value="60" />
                            <ProgressBar Height="8" Maximum="100" Value="60" />
                        </StackPanel>
                    </Border>

                    <!--  Surface layers + badges  -->
                    <Border Style="{StaticResource Card}">
                        <StackPanel joufflu:Spacing.VerticalGap="10">
                            <TextBlock Style="{StaticResource H3}" Text="Surfaces &amp; badges" />
                            <Border Padding="12" Background="{DynamicResource {x:Static joufflu:Brushes.Background100Brush}}" BorderBrush="{DynamicResource {x:Static joufflu:Brushes.BorderBrush}}" BorderThickness="{StaticResource {x:Static joufflu:Dimensions.BorderThickness}}" CornerRadius="{StaticResource {x:Static joufflu:Dimensions.CornerRadius}}">
                                <StackPanel joufflu:Spacing.VerticalGap="8">
                                    <TextBlock Text="Elevated surface (Background 100)" />
                                    <Border Padding="10" Background="{DynamicResource {x:Static joufflu:Brushes.Background200Brush}}" CornerRadius="{StaticResource {x:Static joufflu:Dimensions.CornerRadius}}">
                                        <TextBlock Foreground="{DynamicResource {x:Static joufflu:Brushes.Foreground100Brush}}" Text="Selected surface (Background 200)" />
                                    </Border>
                                    <WrapPanel joufflu:Spacing.HorizontalGap="8" joufflu:Spacing.VerticalGap="8">
                                        <controls:Badge>Default</controls:Badge>
                                        <controls:Badge joufflu:ControlProperties.Size="xs">Small</controls:Badge>
                                    </WrapPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!--  Generated dictionary  -->
                    <Border Style="{StaticResource Card}">
                        <StackPanel joufflu:Spacing.VerticalGap="10">
                            <TextBlock Style="{StaticResource H3}" Text="Generated dictionary" />
                            <Border Style="{StaticResource CardSecondary}">
                                <TextBox
                                    MaxHeight="360"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    FontFamily="Consolas, Cascadia Mono, monospace"
                                    Foreground="{DynamicResource {x:Static joufflu:Brushes.Foreground100Brush}}"
                                    HorizontalScrollBarVisibility="Auto"
                                    IsReadOnly="True"
                                    Text="{Binding GeneratedXaml, Mode=OneWay}"
                                    TextWrapping="NoWrap"
                                    VerticalScrollBarVisibility="Auto" />
                            </Border>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
